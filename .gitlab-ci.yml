stages:
  - supermicro
  - equinix
  - applications

# supermicro deployment
supermicro:
  stage: supermicro
  only:
     variables:
      - $TARGER_SERVER == "supermicro"
  trigger:
    # Include the configuration file of the child pipeline
    include: inventories/supermicro/.gitlab-ci.yml
  rules:
    

# equinix deployment
equinix:
  stage: equinix
  only:
     variables:
      - $TARGER_SERVER == "equinix"
  trigger:
    include: inventories/equinix/.gitlab-ci.yml

# equinix deployment
applications:
  stage: freeipa
  only:
     variables:
      - $DEPLOY_APP == "freeipa"
  trigger:
    include: applications/freeipa/.gitlab-ci.yml
